# entity: photo

Types and mappers for the `photos` table and gallery UI. Constants for allowed MIME types and max size are shared with `@features/gallery-upload` validation and `@shared/ui` `PhotoUploader`.
