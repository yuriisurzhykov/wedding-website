# widget: wishes-section

Section `#wishes`: `@features/wish-list` for SSR feed, client `WishesSectionForm` posting to `@features/wish-submit` via `POST /api/wishes`. Optional image uses the same presign/R2/confirm flow as the gallery (`upload-wish-attachment.ts`).
