/**
 * Widget-internal limits. Not re-exported from the slice public API.
 * Callers pass `presentation` on `WishesSection` instead of importing these.
 */
export const WISHES_LIMIT_PREVIEW = 3;
export const WISHES_LIMIT_FULL = 50;
