# widget: donate-section

Section `#donate`: localized copy and `DeepLinkButton` list from `@entities/payments`. A button appears only when that
provider’s env values parse to valid links or phone (see `@entities/payments` README).
