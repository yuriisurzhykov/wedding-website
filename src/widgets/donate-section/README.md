# widget: donate-section

Section `#donate`: localized copy and `DeepLinkButton` list from `@entities/payments`. Buttons are omitted when env-backed links are empty (except Zelle when `PAYMENTS_ZELLE_PHONE` is set).
