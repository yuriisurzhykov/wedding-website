# Widget: `schedule-section`

Timeline of the wedding day from `@entities/schedule` with localized times via `@shared/lib/wedding-calendar`.

## Public API

| Export            | Role                    |
|-------------------|-------------------------|
| `ScheduleSection` | Async server component. |
